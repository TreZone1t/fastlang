# 🚀 التحليل الشامل لمشروع `fast_lang` وخريطة الطريق

## 🎯 1. فلسفة اللغة والهدف الأساسي
لغة `fast_lang` ليست مجرد لغة برمجة تقليدية، بل هي لغة تعتمد على **الهندسة العكسية للمفاهيم البرمجية (Metaprogramming & Dynamic Scoping)** مع الحفاظ على الأداء الفائق من خلال ترجمة الكود (Transpilation) إلى **C++**. 
الهدف هو بناء لغة تمنح المبرمج تحكماً كاملاً في سلوك الذاكرة وتدفق البيانات (Data Flow) من خلال بنية موحدة وعبقرية تُدعى الـ `Scope`، بدلاً من تشتيت المبرمج بين الكيانات التقليدية (`class`, `function`, `struct`).

---

## 🏗️ 2. المعمارية الهندسية الحالية (Architecture)

يتكون المترجم (Compiler) الخاص بـ `fast_lang` من أربع مراحل أساسية مبنية بلغة **Rust**:

### أ. الماسح اللغوي (Lexer/Scanner)
- **الملفات:** `token.rs`, `scanner.rs`
- **الحالة:** مكتمل وقوي جداً.
- **الميزات الفريدة:**
  - يتعرف على الكلمات المفتاحية السياقية (Contextual Keywords) التي تميز اللغة مثل: `scope`, `flag`, `param`, `blueprint`, `enable`, `event`, `handle`.
  - يدعم الأنواع الأولية (Primitive Types) بشكل أصلي: `int`, `float`, `str`, `char`, `bool`, بالإضافة إلى النوع الجوهري `array`.
  - مصمم للتغاضي عن الأخطاء (Error Recovery) لضمان استمرار التحليل وإعطاء تقارير دقيقة للمستخدم.

### ب. شجرة التحليل النحوي (AST & Parser)
- **الملفات:** `ast.rs`, `parser.rs`
- **الحالة:** متقدم جداً ويدعم النحو الخاص باللغة بالكامل.
- **الميزات الفريدة:**
  - **Pratt Parser:** تم تطبيقه بكفاءة لمعالجة العمليات الحسابية والمنطقية (Infix/Prefix/Postfix) والوصول للعناصر `arr[i]`.
  - **الـ Scope الديناميكي (`ScopeDecl`):** هو القلب النابض للغة. الـ Scope في `fast_lang` يمكن أن يتشكل ليكون دالة `Fn`، أو `Struct`، أو حلقة تكرار `looped`، أو نوع مخصص `custom` (مثل `array` و `str`).
  - **فصل `flags` عن `settings`:** المعمارية تفصل تماماً بين إعدادات مسار التنفيذ (`flags` مثل `is_break`, `is_return`) وبين خصائص الـ Scope (`settings` مثل `index_access`, `public`, `length`) عبر أمر `enable`.
  - **الـ Constructors المدمجة:** دعم أصيل لكتابة دالة البناء `_(params) -> { ... }` داخل أي Scope.
  - **الأنواع المركبة (Nested Types):** قراءة وفهم تام لأنواع مثل `array(int(32))` وتخزينها برمجياً.

### ج. المحلل الدلالي (Semantic Analyzer)
- **الملفات:** `analyzer.rs`
- **الحالة:** في مرحلة التأسيس القوي.
- **الميزات الفريدة:**
  - بناء جدول الرموز (Symbol Table) لتتبع المتغيرات والدوال على مستوى النطاق المحلي (Local) والعالمي (Global).
  - التحقق المبدئي من صحة الأنواع واستدعاء الدوال والتأكد من عدم استخدام المتغيرات قبل تعريفها.

### د. مولد الكود (Code Generator)
- **الملفات:** `generator.rs`
- **الحالة:** يقوم بالترجمة الأساسية إلى C++.
- **الميزات الفريدة:**
  - يحول أنواع `fast_lang` إلى ما يقابلها في C++ (مثلاً `int(32)` إلى `int32_t`).
  - يترجم الـ `custom scopes` مباشرة إلى هياكل `struct` أو `class` في C++، مما يحافظ على سرعة التنفيذ.

---

## 🧠 3. الخصائص الجوهرية للغة (Language Features)

1. **الـ Scope كمكون شامل (Omni-Scope):**
   يمكن للمبرمج تعريف أي شيء عبر الـ Scope:
   ```fast
   scope MyList -> {
       type -> custom;
       enable [index_access, custom_init_body, length];
       add int length;
       _(array(T) arr) -> { ... } // Constructor
       public -> { ... }
   }
   ```
2. **الأنواع الأولية `array` و `str`:**
   يتم التعامل معها كأنواع بدائية ولكنها تمتلك خصائص الـ Scope، مما يمنحها قوة الكائنات (Objects) وسرعة الـ Primitives. الهدف المستقبلي لها هو بناؤها على هيكل القوائم المترابطة (Linked Lists) داخلياً لضمان كفاءة استهلاك الذاكرة.

3. **نظام الـ Events والـ Handles (ميزة قيد التطوير):**
   - السماح بكتابة `event.call -> { ... }` ليتم تنفيذ الكود عند وقوع حدث معين.
   - اعتراض حالات الـ Control flow عبر `handle.is_break -> { ... }`.

---

## 🗺️ 4. خريطة الطريق المستقبلية (Roadmap)

للوصول بـ `fast_lang` لنسختها الأولى المتكاملة (v1.0)، هذه هي الخطط التي يجب تنفيذها:

### 🔴 المرحلة 1: إحكام المحلل الدلالي (Semantic Engine Mastery)
- **تطبيق قواعد الـ Settings:** 
  - تفعيل الدعم الدلالي لـ `index_access` (رفض أو قبول `arr[i]` بناءً على الـ settings).
  - دعم الـ `custom_init_body` لتمكين إنشاء الكائنات بدون أقواس `{}` (مثل `array arr = [1, 2, 3];`).
- **نظام التحقق الصارم (Strict Type Checking):** التأكد من توافق الأنواع المركبة مثل `array(T)` ومنع العمليات غير المنطقية.

### 🟡 المرحلة 2: هندسة الذاكرة والمكتبة القياسية (Memory & StdLib)
- **تطبيق `array` و `str` في C++:** كتابة الطبقة التحتية التي ستحول هذه الأنواع في C++ إلى قوائم مترابطة (Linked Lists) مُحسنة عالية الأداء كما تم التخطيط له.
- **تفعيل `add <type> <name>;` ديناميكياً:** جعل المترجم يدمج الحقول المضافة ديناميكياً (مثل `size` و `length`) داخل الهيكل المولد في C++.

### 🟢 المرحلة 3: نظام الأحداث والماكرو (Events & Handles Compilation)
- برمجة المحول (Generator) ليقوم بترجمة `event` و `handle` إلى C++ Macros أو Callback functions ذكية لا تؤثر على الأداء وقت التشغيل (Zero-cost abstractions).

### 🔵 المرحلة 4: المعمارية المتقدمة للكائنات (Advanced OOP)
- تفعيل الوراثة `extends` والكلمة المفتاحية `super`.
- دعم الـ `blueprint` كبديل مرن للـ Interfaces في اللغات التقليدية.
- استكمال منظومة الـ Error Handling (`throw`, `try`, `catch`).

---

## 📝 الخلاصة
مشروع `fast_lang` هو مترجم متقدم يكسر القواعد التقليدية للبرمجة الموجهة للكائنات (OOP) من خلال توحيد كل الهياكل تحت مظلة الـ `Scope`. المشروع يمتلك الآن أساساً متيناً جداً (Lexer و Parser يعملان بكفاءة عالية)، والخطوة القادمة هي تفعيل الدلالات (Semantics) الخاصة بهذه الميزات الفريدة وتحويلها إلى كود C++ صلب.
