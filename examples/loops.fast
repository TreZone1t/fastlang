// FastLang Example: Loops & Control Flow (.fast)

fn main() -> int32 {
    println("=== 1. Standard While Loop ===");
    int32 i = 0;
    while (i < 5) -> {
        println("While iteration: ", i);
        i = i + 1;
    }

    println("=== 2. Do-While Loop ===");
    int32 j = 0;
    do -> {
        println("Do-While iteration: ", j);
        j = j + 1;
    } while (j < 3);

    println("=== 3. C-Style For Loop ===");
    for (int32 k = 0; k < 4; k = k + 1) -> {
        println("For iteration: ", k);
    }

    println("=== 4. Infinite Loop with Break & Continue ===");
    int32 count = 0;
    loop -> {
        count = count + 1;
        if (count == 2) -> {
            continue;
        }
        println("Loop count: ", count);
        if (count >= 5) -> {
            break;
        }
    }

    println("=== 5. For-In Iteration ===");
    int32 numbers[5] = [10, 20, 30, 40, 50];
    for (val in numbers) -> {
        println("Array item: ", val);
    }

    return 0;
}
