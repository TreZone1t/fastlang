use std::math::{sum};

fn main() {
    let int(32) result = sum(10, 20);
    log("Sum is: ");
    
    // Test that private_helper is not accessible
    // private_helper(); // should fail if uncommented
}