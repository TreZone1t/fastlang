// 01_variables.fs
// Test primitive types and variable declarations

 int32 global_counter= 0;
 const float64 pi = 3.14;
fn make_name() -> name<int32> {
    int32 a = 10;
    return &a;
}
fn main() -> int32 {
    //the feature i want to add:
    //int32 a, b, c;
    //int32 a = 10 , b = 20 , c = 30;
    //int32 z , x, y = 10;
    bool Is_active = true;
    char Initial = 'A';
    Is_active = false;
    Initial = 'B';
    int32 c;
    int32 counter = 0;
    counter = counter + 1;
    counter +=  1;
    c = counter;
    int32 *ptr;
    ptr = &counter;
    *ptr = counter + 1;
    char names[6] = "hakim"; //the size of the array is 6 then the last char is '\0' 
    print(names);
    //name<int32> ptr2 = make_name(); // this is now working but i think there is a room for improvement
    name<int32> ptr2;
    //print(*ptr2);
    ptr2 = &counter;  // this is not working yet for the same reason i think
    print(*ptr2);
    int a = 10; // you can use int as usual
    float b = 10.0; // you can use float as usual
    print(a);
    print(counter);
    print(c);
    return 0;
}
