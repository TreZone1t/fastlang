// Valid: complete custom scope with label , goto , leave , call , data
//update : we removed custom you can use machine instead
machine recursion_chaos -> {
    @init -> {
      int data = 0;
      goto -> @start;
    }
    @start -> {
      if (this.data <= 0) {
        this.data -> 1;
      }else{
        this.data -> this.data * 2;
      }
      goto -> @process;
    }
    @process -> {
      if ( this.data % 2 == 0) {
        this.data -> this.data + 1;
      }else{
        leave;
      }
      goto -> @start;
    }
    handle -> {
        fn call () -> int32 {
            goto  -> @init;
        }
        fn leave () -> int32 {
           return this.data;
        }
    }
}

fn main() -> int32 {
  print("Testing recursion_chaos (leave):");
  print(recursion_chaos()); // Should print 1
  return 0;
}
