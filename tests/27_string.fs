import std::string::{string};

fn main() -> int(32) {
    string s;
    s -> "Hello";
    log("String: ", s);
    log("Size: ", s.size());
    log("First char: ", s[0]);
    log("Last char: ", s[4]);

    return 0;
}
